/**
 * AI Assistant chat page.
 *
 * Accessible to: manager, owner, superadmin.
 * Context: platform data only (learning stats, attendance, test results).
 * Chat history is persisted in backend ai_conversations table.
 */
export default function AIAssistant() {
  return (
    <main>
      <h2>ИИ-ассистент</h2>
      {/* Chat UI */}
    </main>
  );
}
