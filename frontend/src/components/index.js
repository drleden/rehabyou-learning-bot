/**
 * Shared UI components barrel export.
 *
 * Components to be implemented:
 *   - ProgressBar       — course / module / lesson completion bar
 *   - VideoPlayer       — presigned URL video player
 *   - TestWidget        — multiple-choice test with countdown
 *   - BadgeCard         — badge / achievement tile
 *   - UserCard          — employee card (avatar, role, status)
 *   - NotificationBell  — notification icon with unread count
 *   - RoleGuard         — render children only for allowed roles
 *   - Leaderboard       — branch ranking table
 */

// Exports will be added as components are implemented.
